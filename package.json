{
    "private": true,
    "scripts": {
        "init": "git submodule add https://github.com/ygg-lang/project-yggdrasil.git projects/project-yggdrasil",
        "build": "cargo build --release",
        "test": "cargo test --release",
        "fmt": "cargo fmt --all",
        "push": "wee test && git push",
        "reset": "git reset Head~ --soft",
        "pub": "cargo-publish-all --yes --allow-dirty"
    }
}
